---
name: skill-name-here
description: Replace with what this skill does AND when an agent should use it. Include the concrete trigger keywords a user's request would contain (product names, endpoints, header names, error strings). 1–1024 characters.
license: MIT
metadata:
  author: terra
  version: "0.1.0"
---

# Skill title

<!--
Reminders (delete before publishing):
- The frontmatter `name` must exactly match this skill's directory name
  (lowercase letters, digits, hyphens; max 64 chars).
- Keep this file under 500 lines. Move deep material to references/ and link
  each file with an explicit trigger condition, e.g.
  "Read references/foo.md when doing X."
- Gotchas (surprising, environment-specific facts) stay in this file.
- Provide defaults, not menus. Procedures over declarations.
- Register the new skill in skills.sh.json, .claude-plugin/marketplace.json,
  and the README catalog table. See AGENTS.md for the full guide.
-->

## Instructions
